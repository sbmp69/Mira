const { PrismaClient } = require('@prisma/client'); const { PrismaPg } = require('@prisma/adapter-pg'); const { Pool } = require('pg'); const fs = require('fs'); const path = require('path'); require('dotenv').config(); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); const adapter = new PrismaPg(pool); const prisma = new PrismaClient({ adapter }); async function run() { const mira = await prisma.companion.findFirst({ where: { name: 'Mira' } }); if (mira && mira.avatar) { const base64Data = mira.avatar.replace(/^data:image\/\w+;base64,/, ''); const dir = 'E:/Mira/app/assets'; if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true }); fs.writeFileSync(path.join(dir, 'mira_logo.jpg'), base64Data, 'base64'); console.log('Saved'); } else { console.log('No avatar'); } } run().finally(() => prisma.$disconnect());
